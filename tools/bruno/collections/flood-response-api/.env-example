# Copy this file to .env and set the values for your machine.
# Bruno auto-loads .env from this collection root and exposes variables
# as {{process.env.VAR_NAME}}, e.g. the baseUrl in environments/staging.bru.
# .env itself is gitignored.

API_BASE_URL=https://example.org